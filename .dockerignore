# Only pom.xml and src/ are needed to build the image (it builds with the base image's Maven, not the wrapper).
target/
.git/
.gitignore
.dockerignore
.idea/
*.iml
.vscode/
.settings/
.classpath
.project
.DS_Store
*.log
docs/
Dockerfile
docker-compose.yml
